'use client';

import UploadForm from '../components/uploadForm/UploadForm';

export const Upload = () => {
    return (
        <div>
            Upload page
            <UploadForm />
        </div>
    );
};

export default Upload;
